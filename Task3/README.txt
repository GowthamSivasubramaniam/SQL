Objective:

Summarize data using aggregate functions and grouping.
========================================================================================

steps followed 


==> created Table named StudentRegister with columns Rollno , class, department 
==> used to alter query and added column named Name
==> Inserted multiple rows using insert into query
==> Used Select query and retrived the data
==> Used Select query With aggregate function "count(rollno)" and counted how much students are in each department"
==> Used Select query With aggregate function "count(rollno)" and counted how much students are in class A of 
each department here used group by with "Department and Class" and 
used having to count only the students in class "A"

========================================================================================